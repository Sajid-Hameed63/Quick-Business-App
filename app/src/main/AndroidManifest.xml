<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.quickbusiness">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_channel"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QuickBusiness">
        <activity android:name=".Activities.ProductBuyActivity" />
        <activity android:name=".Activities.ProductCreationActivity" />

        <activity android:name=".Activities.ShopCreationActivity" >

        </activity>
        <activity android:name=".Activities.ProductMainPageActivity" >

        </activity>

        <activity android:name=".Activities.ProfileActivity" />
        <activity android:name=".Activities.AboutActivity" />
        <activity android:name=".Activities.RateUsActivity" />
        <activity android:name=".Activities.FollowUsActivity" />
        <activity android:name=".Activities.SignUp_Activity" />
        <activity android:name=".Activities.LogIn_Activity" />
        <activity android:name=".Activities.LogInIntro" >
            <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity android:name=".Activities.MainActivity">

        </activity>
    </application>

</manifest>